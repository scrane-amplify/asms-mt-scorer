---
title: "Molecule Task"
output: html_notebook
---

```{r setup, include=TRUE, echo=TRUE, message = FALSE}
knitr::opts_chunk$set(message = FALSE)
library(tidyverse)
library(here)
library(C50)
library(caret)
source(here("01_tidy", "tidy_molecule.r"))
source(here("02_analysis", "helpers.R"))
```

```{r }
molecule_split <- split_data(data        = molecule_derived,
                             split_ratio = 0.70,
                             seed        = 123)

```

```{r}
molecule_variables <- c("gas_left", "gas_right", "gas_correct", "gas_absent")
```

```{r molecule}
dt_molecule <- C5.0(x = as.data.frame(molecule_split$data[[1]][,molecule_variables]), 
                    y = molecule_split$data[[1]]$molecule, 
                    trials = 3)

summary(dt_molecule)

plot(dt_molecule)
```

```{r concentration}

```

```{r absorption}

```

```{r transfer}

```

